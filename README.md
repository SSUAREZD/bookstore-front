correr el comando npm test en terminal para ejecutar las pruebas.
se guardo en la memoria del buscador a travez de json para persistir los cambios y los datos
se utiliza un filter del campo authors para poider hacer un filtrado